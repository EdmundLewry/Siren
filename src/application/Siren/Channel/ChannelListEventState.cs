namespace PBS.Siren
{
    /*
        A Channel List Event State is the changeable data that relates to an individual Channel List Event,
        such as whether it's currently valid, it's current playout state (is it cued? playing?) and so on.
    */
    public class ChannelListEventState
    {
        public ChannelListEventState()
        {
            
        }
    }
}