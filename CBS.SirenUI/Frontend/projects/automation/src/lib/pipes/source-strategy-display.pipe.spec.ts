import { SourceStrategyDisplayPipe } from './source-strategy-display.pipe';

describe('SourceStrategyDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new SourceStrategyDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
