import { TimingStrategyDisplayPipe } from './timing-strategy-display.pipe';

describe('TimingStrategyDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TimingStrategyDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
