import { FeatureTypeDisplayPipe } from './feature-type-display.pipe';

describe('FeatureTypeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new FeatureTypeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
