import { TimeStringDisplayPipe } from './time-string-display.pipe';

describe('TimeStringDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeStringDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
