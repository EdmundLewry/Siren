import { PlayoutStrategyDisplayPipe } from './playout-strategy-display.pipe';

describe('PlayoutStrategyDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayoutStrategyDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
